{
    "private": true,
    "name": "websocket-game-control-server",
    "description": "A React game control application, powered by WebSocket",
    "version": "1.0.0",
    "main": "index.js",
    "scripts": {
        "start": "npm run build && node build/index.js",
        "build": "rimraf ./build && tsc"
    },
    "dependencies": {
        "ws": "^8.5.0"
    },
    "devDependencies": {
        "@types/ws": "^8.5.3",
        "@types/node": "^18.11.9",
        "rimraf": "^3.0.2",
        "typescript": "^4.8.4"
    }
}
